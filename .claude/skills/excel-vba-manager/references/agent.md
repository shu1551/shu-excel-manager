# agent（依頼文 1 つを道具が回す薄い入口）の全文

> excel-vba-manager の参照。SKILL.md（型と索引）から必要なときだけ読む（2026-09-16 に分割）。

SKILL.md の索引には 1 行しか無い。--fire・--shake・--recipe・--undo・--runs・関所・不変条件の詳しい経緯はここ。

```bash
# 依頼文 1 つを道具が回す薄い入口 agent（2026-09-03・「ループ」を Claude Code から取り上げて Python に持たせた。
#  Excelコンボと同じ形＝道具が材料を先に集め、API の AI に 1 回ずつ聞き、書いて読み戻し、AI が自分の結果を見てから done）
py vba_manager.py agent "郵便番号をハイフンつきに統一して。左の名簿と右の申込一覧を会員番号で突き合わせて F 列に申込コースを足して" --sheet テスト用4
#  入口の振り分け（1 行目「入口: …」で言う。--mode sheet|build|macro で指定できる）:
#    sheet ＝ 既存シートを直す … materials → AI に聞く → write_cells/write_grid/format/tidy → 読み戻し（--show／tidy）→ … → 検査
#    build ＝ 白紙／新しいシートを組む … build-sheet --ask と同じ（--new-book でまっさらなブックに）
#    macro ＝ マクロを修理する … get・call-graph・compile（いまの状態）→ AI に仮説と最小の一手を返させる →
#            code_replace（変更行だけ）か replace（1 本丸ごと・バックアップつき）→ 道具が全体コンパイル → … → done
#            AI に許す手は get / grep / code_replace / replace / add / compile と、読むだけの list（マクロ一覧）/
#            impact（呼び元・呼び先）/ call_graph（2026-09-04 追加）。rehearse（コピーで試し撃ち）と run（本物で動かす）は
#            --rehearse のときだけ＝関所つき。関所が閉じているときは「動かして確かめてはいない」と report に書かせる。
#            マクロ名は --macro か依頼文の中の名前（最長一致）。書き換えは replace-procedure と同じく保存される（戻すなら restore）
#    決め方: --mode > --macro > --new-book > 依頼文にマクロの語 > 「新しいシート」等の語か対象シートが白紙 > sheet
#  sheet の AI に許す手は 44 個（2026-09-06 夜の 42 個に、09-09 に read_file、09-11 に dedupe＝重複行を表記ゆれ込みで照合して下から消す手を足した）: read（"sheet" で他シートも読める）/ write_cells / write_grid / format / tidy と、
#    read_sheet（シートの材料を読み直す。他シートも。旧 inspect）/ eval（式を計算して結果を見るだけ）/ view（枠固定・表示倍率）/
#    page_setup（印刷範囲・タイトル行・向き・横 1 ページ・ページ番号）/ chart（グラフ）/ image（写真を置く）/
#    shape（図の位置と大きさ・削除。マクロ付きの図は道具が拒む。"names":"all_without_macro" でマクロ無しを全部消す）/ export_csv
#    （範囲を CSV ファイルへ。既にあるファイルには書かない）/ read_file（別のファイル csv・tsv・txt・xlsx・xlsm を新しいシートに写す。
#    元のファイルには書かない。先頭ゼロの列は文字のまま。依頼文にパスがあれば道具が先に姿を読んで材料に出す・2026-09-09）。消せるのは範囲の中身（clear_range）・行と列（row_delete／col_delete）・
#    図形（shape の delete）で、どれも承認の言葉があるときだけ。名前・リンクを消す手は無い＝候補を report に書く。
#    表の足回り（2026-09-04 追加）: sort（並べ替え。key＝列文字・desc＝降順）/ autofilter（絞り込みの▼）/
#    cond_format（条件付き書式＝色分け。gt/lt/ge/le/eq/ne/between/formula ＋ bg・color・bold。行が動いても崩れないので
#    format の固定塗りで代用しない）/ validation（ドロップダウン）/ row_insert・col_insert・row_delete・col_delete・row_group・col_group /
#    copy_range / find（読むだけ）/ export_pdf / chart_config（凡例・軸・目盛・ラベル）/ hyperlink / comment /
#    sheet_op（シートを足す・改名・複写・タブ色。消す・隠すは無い）/ name_add。
#    **手の無い依頼を黙って落とすのは禁止**（規則に明記）。依頼を分解し、当てる手が無いものは report で
#    「やったこと」「できなかったこと（人がやるなら何をするか）」に分けて書く。
#    ← 2026-09-04 の実測: 「目立たせて、貸出日の新しい順に並べ替えて」に format 3 本だけ返して並べ替えを黙殺した
#    （消す手だけが規則に「無い」と書いてあったので断れ、書いていない不足は黙って落ちていた）。
#    書けるのは対象シートの中だけ（シート名つき・列全体は道具が弾く）。**手と done は同じ返事に載せてよい**（2026-09-06 夜。
#    全部の手が通れば道具は次の往復を待たずに関所へ進む＝10/14 走行にあった「actions 空の done だけの 2 往復目」（4 秒・
#    13,000 トークン）が消える。1 つでも失敗すれば結果を返して続く）。
#    請求書の下に**道具側の内訳**（材料／控え／実行／画像／仕上げ検査／不変条件／採点の材料／終わりの検査／差分 の秒）が出る
#    （同日。全体の 3〜4 割が道具側なのに AI の秒しか出ていなかった）。採点が通った直後の終わりの検査は、採点に渡した材料と
#    画像を使い回す（同じシートを 3 回読み・4 回撮っていた分を畳んだ）。重い道具（テーブル・ピボット・スライサー・パワークエリ・
#    データモデル）の長い説明は、依頼か材料にその語があるときだけ規則文に入る（規則文 20,000 字の 4 割）。
#    採点係の unmet のうち「人の値を変えないと満たせない」もの（削除・置き換え・半角に・修正 …）は、承認の言葉が無い走行では
#    plan に混ぜず報告の【気づいたこと】へ回す（同日。初実射の往復 5 回の型を機械で塞いだ）。【人に判断してほしいこと】と重なる
#    気づきは畳む。材料の「数値に見える文字」から番号列（会員番号・コード・No・ID）を外す（規則文の「番号は文字のまま」と矛盾していた）。
#    名前定義が KEY／TOKEN／PASSWORD を指すセルの値は、AI に送る文から伏せる（材料に GEMINI_API_KEY = =目次!$A$13 が出ていた）。
#    値の先頭の ' は Excel と同じ「文字のまま」（'0001・'2026/01/05。write-cells / write-range 共通・2026-09-04）。
#    日付の文字（2026/1/5）は日付として入る（tz=UTC で渡す＝素の datetime だと pywin32 が 9 時間ずらす）。
#    FILTER 等の動的配列の数式は Formula2 で書く（.Value だと @ が付いて 1 セルに潰れる）。@ 書式のセルに数値・日付を
#    書くときは標準に戻してから書く（@ のままだと '1/4/2026 3:00:00 PM' の文字で入る）。いずれも実射で出た穴
#    材料は materials ＋「追加の材料」（他のシート・枠固定・行高・印刷・条件付き書式・外部リンク・名前定義の壊れ/未使用・
#    使用範囲の膨張・空白に見えるゴミ・ファイルの大きさ）＝手順書が推測なしで動ける分だけ道具が先に読む（2026-09-04）
#    行が多い表（40 行超）は材料に「列プロファイル」（列ごとの型・乱れ・形・種類）が付き、AI は normalize（列に規則を当てる。
#    trim/hankaku/hyphen/number/date/regex/as_text/fill_down。道具が全行を処理）／fill（数式を末尾まで）／find_replace で直す。
#    write_grid は 100 行まで＝AI に行を触らせない（同日。300 行の練習台 2 本が往復 2・AI の出力 191 トークンで通った）
#    書いた直後の見た目（PNG・使用範囲の左上から 40 行 × 20 列＋余白 2 行 2 列）は **--image を付けたときだけ** AI に見せる
#    （罫線の継ぎはぎ・寄せ・### は文字では見えない。見せると 1 往復あたり 1,000〜1,400 トークン増。2026-09-04 に見せる既定で入れ、
#    2026-09-08 に既定を「見せない」へ反転。--no-image は互換のため残っているだけ）
#    画像には行番号・列番号のヘッダーを描き足す（2026-09-06・PIL。Excel の画面と同じ見た目＝AI が左上から数えなくてよい。
#    描けなければ添え文が「左上のセルが A1」に戻る。人が見る _last_agent.png も同じ）
#    最初の「書く手」の前に控え（SaveCopyAs）を取る → `agent --undo` でそのシートだけ戻せる（保存はしない。
#    シートは消さずに中身を入れ替える＝他シートからの参照が #REF! にならない。図形の位置も戻す。--dry-run で下見）
#    控えはブックごとに 5 個残り、`agent --backups` で番号つきの一覧 → `agent --undo 番号`（走行の名札でも可）で
#    直前より前の控えにも戻せる（2026-09-06・控えごとに覚書を隣に置く）。その控えより後に同じブックで走行が
#    あれば既定で止まる（後からやったぶんが消える＝--force で越える）
#    終わりに「変わったセル N 個」と例を道具が出す（AI の報告を信じずに検分できる）。明細（シート・番地・前・後と
#    書式の変化）は `agent --changes`（MCP は changes=True。Excel に触らない）
#    `agent --continue "置き換えてよい"` で前回の続き（記録の会話を引き継ぐ＝手順書が報告で止まった後の承認に）
#    終わりに検査（エラーセル・###・画像 _last_agent.png。元からあったエラーは数えない）と請求書の合計（往復回数・字数・待ち秒・トークン）を出す。保存はしない
#    終わりに 2 つの関所を通る（2026-09-04・「値は合ったが見た目がひどい」「頼んだ半分で done」を道具で止める）:
#    (0) 落とし物の関所＝一覧に「未」が残ったままの done は受け付けない。ただし**手と done を同じ返事で受け、
#        その回の手が全部通った**ときは突き返さず、道具が「済」に直して検査へ進む（2026-09-08。一覧は手を撃つ前に
#        書かれているので、正直に「未」と書いた回だけが往復を 1 回損していた）。
#    (1) 仕上げ検査＝道具が実物を見る（AI を使わない・タダ）。見た目＝見出しが本文と同じ体裁・罫線の継ぎはぎ・数値列の
#        桁区切り無し・番号列が右寄せ・列幅不足の ###・空の見出し。中身（2026-09-06 夜・初実射で空欄を誰も言わなかった件）
#        ＝エラー値・「合計」「小計」行が上の和と合わない・「合計」列が左の和と合わない（番号列と年は足さない・1 行も
#        合わなければ黙る）・数式の列に直値が混ざる（式の断ち切れ）・数式が最後の本文行まで届いていない・突き合わせの列が
#        9 割以上同じ値（VLOOKUP のキーの型違い）。引っかかると done を
#        受け付けず、見た目は tidy・中身は原因のセルを直すよう促して 1 回だけ差し戻す。表の中の空欄・数値列の文字の数字・
#        番号列の重複は止めずに報告の【気づいたこと】へ必ず出す（値を作って埋めさせない＝人が決める）。
#        先頭行そのものが数値のとき（合計ブロック・年を見出しにした表）も桁区切りを見る（2026-09-08）。
#        単体でも使える → `py vba_manager.py audit A1:E9`（指摘 0 件で終了コード 0。気づきは「気づき:」の行）
#    (2) 採点＝依頼文と現物・見た目の画像を AI に照らさせ、満たしていない点を挙げさせる（1 往復ぶん課金。--no-grade で止める）。
#        挙がった点は「やることの一覧」に「未」で足されるので、直すまで done にならない
#        **採点は別の会話で聞く**（2026-09-06）。前は同じ会話の続きに質問を足していた＝自分の手順も言い分も
#        全部見た相手が採点する形で、追認しやすかった。いまは依頼文・道具が読み直した現物・画像だけを渡す。
#        `--grade-ai claude`（`--grade-model` も）で採点だけ別の AI に振れる＝癖まで別人にする
#    関所の通信簿（2026-09-06）: どの関所で何回差し戻したかを数え、終わりに「関所の差し戻し: 仕上げ検査 1 …
#      ＝ 一発合格／関所で直して合格／不合格」を 1 行出す。--fire もこれを弾ごとに記録し、
#      「合格の中身: 一発合格 N / 関所で直して合格 N / 不合格 N」を出す（59/59 でも全部が関所頼みなら AI は直っていない）
#    覚書: 保存済みブックのシートごとに直近 3 件の依頼と報告を覚え（_agent_notes.json）、次に同じシートを触るとき
#      材料に足す（前回どう決めたかを引き継ぐ。人が手で直しているかもしれないので、材料と食い違ったら材料を優先）
#  macro のループにも同じ関所を 2 つ入れた（2026-09-06）: plan の落とし物（「未」のまま done を受け付けない）と、
#    **頼んでいないコードの変化**（撃つ前に全プロシージャのコードを控え、直すと言った 1 本以外が変わっていたら差し戻す）。
#    code_replace は「そのモジュールの中で search に当たる行を全部」書き換えるので、隣のプロシージャが
#    黙って変わってもコンパイルは通っていた。宣言部の変化（Option Explicit を勝手に足す等）も咎める
#  --max-turns N（既定 4・手順書は 6）／--dry-run（1 往復目の手 JSON を見せるだけ。Excel に触らない）／--ai・--model は build-sheet と同じ
#    （既定 claude-code。--fire も 2026-09-09 から既定 claude-code。金のかかる gemini／claude（API 鍵）は --ai で名指ししたときだけ）
#  記録: _last_agent_ask.txt（1 通目）・_last_agent_log.jsonl（往復の全文＝検分の材料）
#  対象ブックは往復のあいだ固定（保存済みならフルパスで名指し、未保存は Activate）。途中で人が別のブックを
#    アクティブにしても、道具はそのブックに流れない（2026-09-03 実射で別ブックに grep が流れた近い事故の再発防止）
#  macro は修理だけでなく新規作成も（2026-09-04）: 「〜するマクロを作って」なら materials＝モジュール一覧＋既存マクロ名＋
#    compile、手に add（新しいプロシージャを名指しのモジュールへ。名前の重なり・宣言 1 本を道具が検査）。書いたら道具が compile
py vba_manager.py agent --score                                # 実射の点数の履歴（何本中何本・いつ・落ちた弾・一発合格の数）
#  --fire は撃つたびに点数を _agent_score.jsonl に記録し、前回と比べて「退行した弾／直った弾」を出す
#  （2026-09-04・「直しても手応えが出ない」＝直した結果が数字で残っていなかったから）
#  --score の最後に「実運用」の 1 行も出る（下の走行台帳の要約）＝上は練習台の点数・下は実戦の記録
py vba_manager.py agent --runs                                 # 本番の走行台帳（2026-09-06・1 走行 1 行）
#  いつ・どのブックとシート・done か・関所で何回止まったか・何秒か・**人が戻した（undo）か**・**人が保存したか**。
#  実射（--fire）と build の直しループは載せない＝練習台の走行で実戦の数字を薄めない。
#  undo 率が本番でいちばん正直な失敗率（done 率は AI の自己申告に寄る）。「保存された（受け入れ）」は
#  ブックの更新日時が走行より新しいとき＝道具は保存しないので、人が保存した（macro は道具が保存するので数えない）
#  走行ごとに往復の全文も _agent_logs\ に控えるので、`agent --replay その記録` で無料で撃ち直せる
py vba_manager.py agent --shake [記録] [--only 名前,名前] [--dry-run]   # 揺らし（2026-09-11 夜）: 記録の返事を Python で書き換えた版
#  （消す手の形 5 通り・消した後の番地・範囲 300 行・書体落とし・表示形式の当て忘れ・overwrite なし・keys 3 通り・tidy なし・
#  順が逆・done を次の往復で…22 通り）を作り、開いているブックに 1 通りずつ再生する。**AI は呼ばない**（1 通り数秒・費用ゼロ）。
#  毎回、保存せずに閉じて開き直す＝同じ汚れた姿から。台帳・控え・覚書は _agent_shake\日時\_state へ逃がす（本物に書かない）。
#  見るのは「一発合格か」と「原本の再生と同じ表か」。一発でない版＝道具が吸収できない揺れ＝直す候補。
#  作れるのは知っている形の組み合わせだけ＝新しい落ち方が出たら vbam_shake._VARIANTS に 1 つ足す
#  --imagine N を足すと、AI（既定 claude-code の haiku＝頭と癖の違う方）に「別の AI ならこの返事をどう書くか」を N 通り
#  書かせて版にする（1 往復・表は直させない）。Python の型に無い揺れを安く広げる。想像と本物の揺れは違うので、ときどき実射で照らす
py vba_manager.py agent --keep-case "観光だけ抜き出す"           # 直前の本番の走行を弾にする（2026-09-06）
#  そのブックを写し取って（--harvest と同じ＝値はダミー・構造は本物）弾ごとの練習台にし、依頼文と
#  シートを _agent_cases.json に控える。**本番で外した「その依頼」が、次からテストになる。**
#  判定は写し取りと同じ＝壊していないか（頼んでいない変化・仕上げ検査の増分・2 回目の変化）＝正解の表は要らない。
#  弾にできるのは sheet／both の走行だけ・そのブックを開いたまま・一度保存してから
py vba_manager.py agent --cases                                # その弾の一覧
py vba_manager.py agent --drop-case "観光だけ抜き出す"           # その弾を消す（台帳の項目と、弾の置き場の練習台）
py vba_manager.py agent --forge 合計行 [--register]              # 鍛える回路（2026-09-17・vbam_forge.py）: AI がマクロを鍛えて増やす
#  直前の走行（先撃ちのマクロの後に AI が残りをやった走行）を弾にする＝撃つ前の控え＋終わりの姿（いまのシートの値）＋
#  依頼文＋AI が使った手。AI にその残りの型をマクロ 1 本（引数なしの Sub・Function なし・番地を直書きしない）として
#  書かせ、撃つ前の写しを自分の Excel で開いて 先撃ち → そのマクロ の順に撃ち、AI の後の表と値で突き合わせる。
#  不一致は AI に返して --max-turns（既定 3）まで。合格した .bas は _agent_forge\名前.bas。check-bas を通ったものだけ読み込む。
#  --register で「表を整える」を持つ開いているブック（1 冊だけのとき・--to ブック名 で名指し）の標準モジュール「表の整理」の
#  末尾に足して全体コンパイル。アクティブなブックには足さない（9/17 の取り違え）。.xlam へは保存 → run-macro アドインの更新登録 --raw。
#  登録簿は同じ Sub 名を 1 本に畳む（開いている .xlsm を先に採る）＝.xlsm と .xlam の両方にあっても 2 回撃たない。
py vba_manager.py agent "依頼文" --forge 名前 --before 前.xlsx --truth 正解.xlsx [--sheet 名] [--test 前2.xlsx 正解2.xlsx ...]
#  人が用意した 2 冊（直す前・人が直した正解）から鍛える（agent の走行は要らない＝職場の作業は 2 冊あれば鍛えられる・9/17 夕）。
#  --truth だけ＝直前の走行を弾にしつつ、正解は AI の後の姿でなく人の表にする。--test＝形が同じで行数・並び・件数が違う表でも
#  AI なしで撃ち、外れたら「別の表でこう外れた」を AI に返す（番地・行数・科目名の直書きを落とす）。合格は全部の表がそろったとき。
#  AI には同じブックのほかのシートの頭（対応表など）も渡す。
#  --phrases 言い換え.txt（1 行 1 つ）＝「依頼の語」が全部に当たるまで鍛える（合格済みでも当たらなければ鍛え直し）。
#  外れの説明には、外れた行の正解の中身と「原因の手がかり」（行が多い＝合計行を拾っている、など）が付く。
#  撃つ前に試しのブックを全体コンパイル（コンパイルエラーは行の名指しで返る・窓で固まらない）。
#  End Sub の後ろに Function を付けた返事は規則違反で返す（黙って切り捨てない）。不合格の弾の撃ち直しは、
#  前回のマクロを今の表で撃ち直した外れから続ける。
#  先撃ち（入口）: 登録簿のマクロは人の Excel で撃つ前に、ブックの写し（SaveCopyAs）へ自分の Excel で試し撃ちする。
#  実行時エラー・時間切れ・何も変えずに終わった、なら人のブックでは撃たず AI に回す。
#  依頼の語が複数の仕事に当たったら、長い語で当たった仕事を優先（見出しがそろわず撃てない仕事の語の方が長ければ、
#  短い語で当たったマクロは撃たない）。
#  職場の型の練習台と確かめ: 作業ファイル\project\fixtures\（振り直し・突合・集約・壊れ）。各フォルダの make_pairs.py が
#  前・正解の組を作る（--unseen N で見ていない種）。check_unseen.py <フォルダ> <名前>＝鍛えたマクロを AI なしで当てる。
#  check_entry.py <フォルダ>=<名前> …（名前! は前提の欠けた表）--requests <名前>＝配った .xlam の登録簿で、
#  依頼（台帳＋phrases.txt の言い換え）× 表 の組み合わせ全部を入口から撃つ（当たる表は値がそろう・ほかは変わらない）。
#  マクロの頭の行が登録簿（' 依頼の語: 合計|集計 ／ ' 扱う: 合計 集計 ／ ' 見出し: 品名 数量 金額）。先撃ち（vbam_prefire）はこれを読んで、
#  次からその語の依頼は AI なしで撃ち、「扱う」の仕事は AI に回さない＝AI の出番は新しい型に出会った一度だけ。--dry-run は AI に渡す文だけ
#  ' 見出し: はシートの上 15 行に全部あるときだけ撃つ（「集計」の語だけで別の表に撃ち、集計を AI に回さなくなる誤爆を防ぐ・9/17 夕）。
#  鍛えるときは必須で、道具が「撃つ前の表・別の表にその語があるか」「依頼の語がこの依頼文に当たるか」を確かめる。
#  試し撃ちは台（鍛冶_撃つ）越し＝実行時エラーは番号と説明で AI に返し、60 秒で打ち切る。同じセルが 2 往復続けて外れたら
#  AI を呼ばずに止める（正解の表から読めない決まり＝並び順など。依頼文に書き足して撃ち直す）。
#  ThisWorkbook・CreateObject（Scripting.Dictionary 以外）は規則で落とす（アドインに入る・職場の PC に .NET 3.5 が無い）
py vba_manager.py agent --forged                               # 鍛えたマクロの一覧（弾・合格・登録先）
py vba_manager.py agent --mend [--seed N] [--only 名,名] [--kinds compile,runtime,nothing,wrong,edge,double] [--dry-run]
#  修理の試験（2026-09-17 夜・vbam_mend.py）: 鍛えたマクロ（前の表・正解・別の表を持つ）を Python が字面で壊し
#  （End If／Next 抜け・ラベル名・メンバー名・Set 抜け・探す語の打ち間違い・変数名の打ち間違い・For の 1 ずれ・比べ方の反転・
#  列のずれ・文を 1 行抜く）、写しで撃って症状を確かめてから、人の言い方の症状だけ（「F8 は「対応なし」のはずが空」）を添えて
#  macro モード（rehearse あり）に直させる。直ったマクロは前の表と別の表の全部で正解と突き合わせる＝done とは別に数える。
#  edge＝いつもの表では合うのにある表でだけ外れる壊し方／double＝数が合わない＋コンパイルエラー。--dry-run は壊し方を選ぶだけ（AI なし）。
#  --from 置き場 [--only 題の id] で前の回と同じお題を撃ち直す（直した後は外れた題だけ）。点数は _agent_mend.jsonl。
#  状態（台帳・控え・覚書）は置き場へ逃がす＝本番の記録に触らないので agent の錠を取らない。
#  macro モードの決まり（この試験で入れた）: code_replace は search／replace に改行を入れてよい（並びごと 1 か所に当たれば
#  道具がプロシージャ 1 本の差し替えに組み直す＝全文を送り直さない）。replace で元の行・注記が消えたら数を結果に出す。
#  「結果が合わない・何も書かれない」の依頼は、直した後に rehearse を撃つまで done を受け付けない（2 回まで）。
#  依頼の「F8 は「対応なし」のはず」は道具が試し撃ちの後の値と突き合わせて ○× を結果に出す。
#  repair は鍛えた台帳（_agent_forge.json）に同じ Sub があれば「鍛えて登録した時の正本」との差分も出す（前は動いていた＝まずここ）。
#  --blind はその差分を出さずに撃つ（台帳に無い手書きのマクロと同じ条件）。
#  結果型の依頼（期待が書いてある）は直す前に道具が rehearse を 1 回撃ち ○× を材料に付ける。直しの後に ○ が増えない・減ったら
#  結果に「関係なかった可能性が高い＝{"op":"undo"}」と出る。undo は直し 1 つ分（replace／code_replace の前の姿）を戻す。
#  --only 表を整える で 表の整理.bas の「表を整える」（約 700 行）も載る（試験の表 5 枚を種 7 で作り、元のマクロの結果を正解に）。
#  macro モードの読む手: {"op":"lines","from":650,"to":720,"module":"表の整理"}（行番号つき・160 行まで）／get にラベル名で GoSub の塊。
#  repair の check に「GoSub の渡し忘れ」（ほかの呼び出しでは直前に入れている受け取りの変数を、ここだけ入れていない）と
#  「入れる前に読んでいる」（Dim した変数を、その行より前で一度も入れていない。Option Explicit なしでは止まらず結果だけ違う）。
#  依頼の「7 列のはずが 8 列」「N 行のはず」も期待として試し撃ちの使用範囲と比べる。done の後、直しが 2 か所以上なら道具が 1 か所ずつ戻して
#  試し撃ちし、戻しても期待が全部 ○ の直しは要らない直しとして戻す（報告に「道具: 要らない直しを戻しました」）。
#  rehearse は撃つ前に全体コンパイル（通らなければ窓を出さずに即返す）。
py vba_manager.py agent --fire mine                            # 本番から拾った弾を全部撃つ（1 本だけなら弾の名前）
#  名前を何も書かない全部撃ちには混ざらない（撃つのは mine か名前を書いたときだけ）
#  組は sheet（決まった仕様・27 本）／vague（曖昧な依頼＝仕様が書かれていない 10 本）／weak（弱点の弾 10 本＝Claude for Excel の
#  失敗型と「止まるべき弾」・2026-09-06）／macro（10 本）／recipe（26 本）で計 83 本、ほかに mine（本番から拾った弾）。
#  vague は「見やすくして」「未入力が分かるように」「合計を出して」「多い順に並べて」「担当ごとの件数」
#  「印刷したら切れる」「鈴木さんの分だけ」など。答え合わせは全部、実物の機械判定（見た目は audit・値はセル）
py vba_manager.py agent --recipes                              # 手順書（sheet の定型 22 本・vbam_recipes.py）の一覧（名前と「いつ」）
py vba_manager.py agent --recipe 値を直す --sheet 名簿            # 手順書を依頼文にして回す（補足なし＝整形列を作って報告で止まる）
py vba_manager.py agent --recipe 値を直す 置き換えてよい          # 後ろの文は「補足」＝承認の言葉・番地・仕様（置き換えてよい／埋めてよい／
#    切ってよい／並べてよい、「集計表は F1 から」「キーは会員番号」「出力先は C:/x.csv」等）。承認が無い手順書は一覧を作って止まる
#  22 本: 値を直す・表と数式の点検・月次の集計表・重複チェック・説明書きを作る・グラフ付き報告・検索と抽出・印刷とPDF・
#    図形と画像の整理・見た目を直す・リンクと外部参照の解消・名前付き範囲の整理・重いブックを軽くする・住所と郵便番号の整形・
#    2つの表の突合・他システムに渡すCSVを作る・帳票を一覧表に直す・並べ替えと絞り込み・色分け・入力ミスを防ぐ・
#    別ファイルとの突合・コード対応表で振り直し（2026-09-06 夜に 25 本を 20 本へ切り直して名前を人の言葉に寄せ、09-09 に
#    read_file を使う後ろの 2 本を足した。並べ替えと絞り込み・色分け・入力ミスを防ぐは 2026-09-04 に**「人が最初に言う言葉」から
#    先に決めて、手を後から合わせた**もの。それまでは道具の持ち手の側から書いていたので、持っていない手の穴＝
#    「並べ替えて」の黙殺を 1 本も捕まえられなかった＝テストが自作自演になっていた）
#  型: いつ／ゴール／手順（完了条件つき）／承認（何があればどこまでやるか）／報告。消すのは人（候補とコマンドを報告に書く）
#  ★撃つ範囲の決まり（2026-09-09・shu 指示。それまでの「共通部分を触ったら全弾」は廃止）:
#    **まず pytest**（2026-09-10）: 直した所が道具の中だけ（関所・手の翻訳・不変条件・控え・台帳）なら撃たずに pytest で
#    確かめる。実射は AI の判断（手の選び方・式の書き方）を見るときだけ。claude-code で撃っても追加料金は無いが、
#    サブスクの使用量は減る＝タダではない。
#    **撃つのは、直した所を通る弾だけ**（5〜8 本）。**網羅（全弾・--repeat・--state の総当たり）は
#    shu が名指しで頼んだときだけ。** 実測: gemini の実射 108 回 2,488 発のうち、69 回 1,404 発が
#    「全部合格＝何も見つけていない」回だった。落ちた 230 発も同じ弾の撃ち直しが大半。
#    撃つ前に**どの頭で撃つかを出力に出す**（既定は claude-code。gemini は金がかかる＝名指しのときだけ）。
py vba_manager.py agent --fire [sheet|vague|weak|macro|recipe|mine|名前 ...]   # 自動実射: sheet 27 本（練習台の名簿＋申込一覧に 郵便番号の統一・
#  会員番号で突き合わせ・金額の合計 など）＋ macro 10 本（一時ブックに壊れたマクロを植えて コンパイルエラーの修理・打ち間違いの修理・
#  存在しないマクロ呼びの修理 など）＋ 手順書の練習台 26 本（汚れた表・壊れた式・散らかった図形・外部リンク・結合セルの帳票などの練習台を
#  まっさらなブックに組んで撃つ）。セル・図形・印刷設定・出力ファイルの現物で答え合わせ→PASS/FAIL→保存せず閉じる。
#  手順書を足したり直したりしたら --fire recipe（弾は vbam_recipes.py の RECIPE_CASES＝build と check を 1 本ずつ持つ）
#  MCP からは agent(request, sheet, mode, macro, max_turns, dry_run, new_book, ai, model, recipe, cont, undo, force,
#    backups, undo_to, image, no_image, no_grade, grade_ai, grade_model, score, runs, cases, keep_case, drop_case, rehearse,
#    recipes, request_file, wait, changes, prune_days) のツールで同じ（recipe=手順書の名前、そのとき request は補足）。
#    道具の .py を直したら **reload_tools()（か vba("reload")）で読み直す**＝サーバーの再起動は要らない（2026-09-06 夜。
#    vba_mcp_server.py 自体を変えたときだけ再起動）。agent の説明文は要点だけ（長くて途中で切れていた）＝詳細はこの段。
py vba_manager.py agent --fire sheet --repeat 3                # 同じ弾を 3 回ずつ撃って弾ごとの合格率（揺れ）を出す（2026-09-06 夜）
py vba_manager.py agent --fire sheet --image                   # 画像を見せて撃つ＝既定（画像なし）の点数・秒と比べる（--grade-ai claude も同じ口）
py vba_manager.py agent --prune-days 60                        # 控えの置き場で 60 日より古いファイルを数えて見せる（--force で消す。
#    agent の間引きは自分の控え 5 個だけ＝他の控えが 3,615 個溜まって一覧が 120 秒を超えていた・2026-09-06 夜）
#  --fire に組の名前（sheet/macro/recipe）と弾の名前を混ぜたら、弾の名前だけを撃つ（2026-09-04）。
#    `--fire recipe 色分け` は組が効いて 25 本全部になっていた＝失敗した弾だけ撃ち直す決めを道具が壊していた
#  同時に 2 つの agent は走らない（_agent_running.lock。MCP と CLI を同時に回すと _last_agent_log.jsonl が混ざり
#    --continue が別の仕事の会話を読み戻す。錠は PID つきで、書いたプロセスが死んでいれば次は通る・2026-09-04）
#  実測（2026-09-03・テスト用4 の A3 の注文まるごと）: 往復 2 回・待ち 7.9 秒・トークン 入力 5,743／出力 653／思考 1,424・合格

# 実射を「当てる目」から「壊していないかを見る目」へ（2026-09-05・弾を 1 本も足さずに効かせる 4 つ）
#  背景: 弾 59 本が全部一発 PASS なのに、本物のブック（文書件名簿）に向けたら欠陥が 5 件出た。
#    原因は本数ではなく練習台の作り方だった＝どれも「絞り込みなし・隠れ行なし・書式が揃った」きれいな表から
#    始まり（_FIRE_BASE の "freeze": False, "filter": False。大きい表の弾すら撃つ前に tidy を当てていた）、
#    答え合わせも「F 列が期待どおりか」しか見ていない（同じ往復で隣の列を全角にしても・他のシートに書いても PASS）。
#  ① 不変条件の検査（全弾に自動・タダ・弾を書き足さない）: 撃つ前に控えを取り、「頼んでいない変化」を咎める。
#    他のシートが変わった／シートや名前定義が消えた／隠れていた行が変わった／元からあった数式が値に潰された／
#    条件付き書式・入力規則が減った。弾ごとの正当な例外は case['inv_skip']。
#    他シートの数式セルは再計算で値が動くので、値でなく数式そのもの（#REF! に化けていないか）で見る。
#  ② 練習台の状態 --state / --seed: 弾はそのままに、撃つ前のシートを本物寄りにする 8 種
#    （隠れ行・前のフィルタ（_FilterDatabase の残留）・枠固定・名前定義・条件付き書式・他シート参照・
#    結合タイトル・書式ばらばら）。無指定なら弾ごとに seed から選び、出力の seed でそのまま再現できる。
#    状態は「答えを変えない」ものだけ＝行や列を挿入しない（番地で書かれた弾が全部ずれる）。
#    状態に消す手を持たせない: Worksheet.Delete は確認ダイアログを出し、実射が丸ごと固まる
#    （2026-09-05・vague 9 本の 2 本目で 12 分停止。Excel は「応答あり」のままなので画面を見るまで分からない）。
#  ③ 冪等性 --twice: 合格した弾に同じ依頼をもう 1 回撃つ。済んだ仕事なので何も変わらないのが正しい
#    ＝合計行の二重足し・列の二重生成・ピボットの二重作成を、正解の表を持たずに捕まえる（往復は 2 倍）。
#  ④ 構造の写し取り --harvest 本物のブックのパス: 値を一目で偽物と分かるダミーに差し替え、構造
#    （結合・絞り込み・隠れ行・数式・名前定義・条件付き書式・入力規則・保護・印刷設定）はそのままの練習台を作る。
#    元のブックは開かない（ファイルを複製して複製の側だけ書き換える）。同じ元値は同じダミーへ写すので
#    突き合わせの関係は壊れない。数値は桁数を保つ（### と桁区切りの検査が意味を持つ）。日付は同じ年月。
#    見出しは既定 1 行だけ残す（--keep-rows）。自分の想像で組んだ練習台の外から的を供給する口。
```
