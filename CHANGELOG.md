# 変更履歴

## 0.2.1（2026-09-24）— Excelコンボの先撃ちが空振りしたら AI に回す

- Excelコンボで先撃ち（棚のマクロを AI より先に撃つ）の確認に［はい］と答えたあと、撃ったマクロがシートを何も変えなかった
  （相手の表が見つからなかった・もう済んでいた）ときは、その頼みを AI に回すようにしました。前は撃って終わりで、
  棚のマクロは相手の表が無いと黙って抜けるため、空振りでも「◆ マクロ…AI 0回」と成功したように見えていました
- 比べるのは、撃ったシートの全セルの式と値・列と行ごとの書式・ブックのシートと図形・テーブル・ピボット・グラフの姿です。
  人に聞くマクロ（取り消しも答え）とファイルに書き出すマクロ（シートが変わらないのが正しい）は比べません
- 経過に「△ 空振り: マクロ名 → AI に回します」と出し、AI には「どのマクロが済み、どれが空振りしたか」を添えて渡します
- 空振りした依頼は「外れた依頼を見る」に［空振り: マクロ名］付きで並びます（そのマクロを直す候補）
- ［マクロ］タブの指示で棚のマクロを撃ったときも、シートが何も変わらなければ経過に「△ 空振り」と出します

## 0.2.0（2026-09-24）— 棚撃ち式・棚のマクロの総点検と改名・AI の窓

- **棚撃ち式**: Claude が表の整理のマクロ（棚）を選んで撃てるようにしました。`shelf`（目録・`--ask 依頼文` で 1 本選ぶ）・
  `shelf-run 名前 [--select 範囲]`（控え→撃つ→差分。戻すのは `agent --undo`）・`trace セル`（式の元を番地の木で）・`seiri`（表を整えて残りだけ報告）
- **棚のマクロの名前を変えました（約 120 本）**。一覧で切れないよう 16 字以内・列を選ぶものは「選んだ〜」・「消す」は「削除」。
  **0.1 のマクロ名でショートカットやボタンを付けていた方は付け直してください**
- 棚をモジュール 4 本に分けました（`表の整理`・`_作る`・`_調べる`・共通の `_下請け`）。`macros/` の .bas も 4 本です
- 棚の総点検で直した主な穴: 21 億を超える数・13 桁の番号でのオーバーフロー／表の右に結果を出すマクロが右のメモを消していた／
  グラフを作るマクロがシートのほかのグラフを消していた／シートを作るマクロが同じ名前の人のシートを消していた／
  「会計課」「計画課」を合計の行と取り違えていた／小計・合計の行の塗り・太字・二重線を表を整えるで崩していた
- アドインに AI の窓（Excelコンボ・AI数式）を入れました。Claude Code 専用（API キーは要りません）。Gemini には対応していません
- 棚と重なる古いメニューのマクロ 13 本と、使わなくなったマクロ 3 本（アクティブな列の最終行へ移動する・シートのクリーン化 2 本）を削除しました
- 試験のデータの電話番号を、実在しない番号（市内局番が 1 で始まる形）に替えました
- 道具: patch-procedure が字下げの違いを許す・古いモジュール名でも見つける・呼び出しの台帳に失敗の理由（`stats`）

## 0.1.1（2026-09-23）— 表を壊す不具合の直し

- `agent --undo`: 戻すときに、文字で入っていた数字を数値・日付に化かしていたのを直しました（「1,000」→1000・「0021」→21・「4-1」→日付）。
  控えの .Formula で範囲の全部を書き直していたためです。入れ直すのは、外部リンクに化けた数式のセルだけにしました。
- 書く道具（write-cells・write_grid・write-range）: 先頭が 0 の数字（伝票番号「0004」・郵便番号「007」）を数にして 0 を消していたのを直しました。
  文字のまま書きます。
- `requirements.txt`: mcp を 1.x に固定しました（2.x は FastMCP が改名されていて、MCP サーバーが起動直後に落ちます）

## 0.1.0（2026-09-18）— 秀エクセルマネージャーとして公開

前身の「秀VBAマネージャー」（https://github.com/shu1551/shu-vba-manager ・2026-09-05 の姿で凍結）から、
手元で開発中の道具一式を、そのまま出したものです。研究中・仕様は変わります。

- MCP サーバーの名前を `vba-manager` から `excel-manager` に変えました（ファイル名と道具の名前はそのまま）
- AI エージェント（agent）を 11 本に分割。マクロの先撃ち（登録済みのマクロが AI より先に撃つ）
- 正解の表を持つ試験（agent --exam）・記録の揺らし（agent --shake）
- 鍛える回路（agent --forge）: AI がやった仕事を AI がマクロに書き、正解つきのお題で試し、合格したら登録する
- 修理の試験（agent --mend）: 合格済みのマクロを 1 行壊し、人の言い方で AI に直させ、全部の表で確かめる
- グラフ・ピボット・テーブル・クエリの姿を読んで比べる目（vbam_objects）
- 修理を 1 手に（repair）・コンパイルエラーの行を名指し・閉じたブックの VBA を読む手（versions / history / list-file / grep-files）
- 開発向けの手（rename-procedure・diff-module・references・copy-modules・set-shortcut・format-module・backup-prune・status）
- 数式とデータの診断（diagnose）・書式の地図（style-map）・呼び出しの台帳（stats）
- 表の整理のマクロ（`作業ファイル/project/macros/表の整理.bas`）
