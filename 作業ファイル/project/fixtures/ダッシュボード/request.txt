選んでいる 3 列（1 つ目を行、2 つ目を値、3 つ目をスライサーの列）で、新しいシートにダッシュボードを作ってください。新しいシートの名前は 行の列の見出しと「別ダッシュボード」をつないだ名前。A3 にピボット（行は 1 つ目の列、値は 2 つ目の列の合計・表示形式 #,##0）、その右にピボットグラフ（集合縦棒・タイトルは 行の見出しと「別」と値の見出しをつないだ文字・凡例なし）、グラフの右に 3 つ目の列のスライサーを置く。前のスライサーは消す。元の範囲は明細の見出しの行から最後の明細の行まで（下の合計の行は入れない）。同じ名前のシートが既にあれば消して作り直す
