選んでいる 2 列（1 つ目＝項目の列、2 つ目＝数の列）で、項目ごとの合計の表を明細の右に 1 列空けて作ってください。見出しの行は明細の見出しの行と同じ行で、見出しは元の 2 列の見出しをそのまま使う。項目は明細に出てくる順（初めて出た順）に 1 行ずつ並べ、値は =SUMIF(項目の列の本文, 項目のセル, 数の列の本文) の式（本文の範囲は $ を付けた絶対参照）。いちばん下に合計の行を足し、左に「合計」・右に =SUM で作った値の合計を入れる。明細の本文は見出しの次の行から最後の明細の行まで（明細の下に合計の行があればそれは入れない）。同じ表が既に右にあれば作り直す。
