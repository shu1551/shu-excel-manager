選んでいる 2 列（1 つ目を項目、2 つ目を値）で、新しいシートに構成比のピボットテーブルを作ってください。新しいシートの名前は 項目の列の見出しと「別構成比」をつないだ名前。その A3 にピボットを置く。行は項目の列、値は 2 つ目の列の合計（表示形式 #,##0）と、同じ列の列集計に対する比率（表示形式 0.0%）の 2 つ。項目は合計の大きい順に並べ、総計あり。元の範囲は明細の見出しの行から最後の明細の行まで（下の合計の行は入れない）。同じ名前のシートが既にあれば消して作り直す
