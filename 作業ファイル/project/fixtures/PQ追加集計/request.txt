パワークエリで、アクティブなシートのテーブルと見出しの集まりが同じテーブルを、このブックから全部追加して 1 つにし、選んでいる 2 列（1 つ目＝文字の列を行、2 つ目＝数の列を値）で集計してください。見出しが違うテーブル（見出しの数や語が違うもの）は入れない。列の並びが違うだけなら見出しで合わせて足す。列は 1 つ目の列・2 つ目の列の合計の 2 列、1 つ目の列の昇順。新しいシート（名前は 元のテーブル名 と _集計 をつないだ名前）の A1 に同じ名前のテーブルとして読み込む。クエリ名も同じ。同じ名前のクエリやシートが既にあれば消して作り直す
